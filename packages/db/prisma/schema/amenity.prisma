model Amenity {
  id     String  @id @default(cuid())
  slug   String  @unique // ex: "Piscine", "Parking", "Spa", "WiFi gratuit"
  icon   String  
  hotels Hotel[] 
  rooms  Room[]  

  translations Json? // 🔥 stocke les traductions en JSON pour éviter les jointures complexes

  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
}
