model Terms {
  id        String @id @default(uuid()) 
  type      TermsType
  translations Json
  version   String
  createdAt DateTime @default(now())
}

enum TermsType {
  CGU
  CGV
  PRIVACY_POLICY
}