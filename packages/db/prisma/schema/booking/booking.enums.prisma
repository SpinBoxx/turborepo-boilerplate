enum BookingQuoteStatus {
  ACTIVE
  EXPIRED
  CONVERTED
  CANCELLED
}

enum PaymentAttemptStatus {
  PENDING
  PROCESSING
  SUCCEEDED
  FAILED
  CANCELLED
  EXPIRED
}

enum PaymentProvider {
  ORANGE_MONEY
  MANUAL
}

enum DiscountType {
  PERCENTAGE
  AMOUNT
}

enum BookingStatus {
  PENDING_VALIDATION
  CONFIRMED
  REJECTED
  CANCELLED
  CHECKED_IN
  CHECKED_OUT
  NO_SHOW
}

enum BookingEventType {
  QUOTE_CREATED
  QUOTE_EXPIRED
  QUOTE_CANCELLED
  QUOTE_CONVERTED
  PAYMENT_ATTEMPT_CREATED
  PAYMENT_PROCESSING
  PAYMENT_SUCCEEDED
  PAYMENT_FAILED
  BOOKING_CREATED
  BOOKING_PENDING_VALIDATION
  BOOKING_CONFIRMED
  BOOKING_REJECTED
  BOOKING_CANCELLED
  BOOKING_CHECKED_IN
  BOOKING_CHECKED_OUT
  BOOKING_NO_SHOW
  REFUND_RECORDED
}